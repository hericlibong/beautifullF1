# --- Environnements & caches
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
.DS_Store
.env

# --- Bac à sable local (non versionné)
test_computing/

# --- Drafts internes à chaque projet (non versionnés)
projects/*/drafts/

# --- Modules mis en pause (doivent rester non suivis)
projects/quali_duels/
projects/wdc_projection_repo/

# --- Artefacts de build / packaging
dist/
build/
*.egg-info/
.coverage

# --- Règle générique pour outputs des projets
projects/*/outputs/**
!projects/*/outputs/*.csv
!projects/*/outputs/*.json

# --- (Nettoyage des anciennes règles spécifiques)
# (ex-typo) hamilton_mildseason_tracker — supprimé, remplacé par règles génériques
# EOF ghost line supprimée
